
box <- 9
box2 <- "car"

box+2

boxes <- c(2,3,4)
boxes+3

class(boxes)
class(box2)

boxes <- NULL

rm(box)

a_vector <- c("Esben", "Anne-Line", "Laerke", "Soren")
a_vector <- c(a_vector, "Anita", "Fabio")
a_vector [2]
a_vector[8]

new_vector <- a_vector[-1:-3]

length(new_vector)

vector_numbers <- c(1,5,89,100)
thesum <- sum(vector_numbers)


data.frame()